# Docker Compose

Executes Bolts into Docker Compose projects.

## Things Still Missing

- Use `provider` services for resource requirement processing. Currently requirements are _not_ processed.
- No Settings management.
